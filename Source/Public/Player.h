#ifndef PLAYER_H\n#define PLAYER_H\n\n#include <vector>\n#include <string>\n\nclass Player {\npublic:\n    Player(const std::string& name) : name(name), health(100), mana(100) {}\n\n    void drawCard();\n    void playCard();\n    void takeDamage(int damage);\n    void restoreHealth(int healthPoints);\n    void addMana(int manaPoints);\n    void useMana(int manaPoints);\n\n    int getHealth() const { return health; }\n    int getMana() const { return mana; }\n    const std::vector<std::string>& getHand() const { return hand; }\n    const std::vector<std::string>& getDeck() const { return deck; }\n\private:\n    std::string name;\n    int health;\n    int mana;\n    std::vector<std::string> hand;\n    std::vector<std::string> deck;\n};\n\n#endif // PLAYER_H